# はじめてのセットアップ手順（パソコン初心者向け）

このページの通りに上から順にやれば、Telegram に通知が届くところまで行けます。
**専門用語はそのつど説明します。分からない単語が出てきても、書いてある通りに入力すれば大丈夫です。**

所要時間は 30〜60 分くらいです。

---

## 目次

- [ステップ 0：全体像を知る](#ステップ-0全体像を知る)
- [ステップ 1：Node.js をインストールする](#ステップ-1nodejs-をインストールする)
- [ステップ 2：ターミナルを開く](#ステップ-2ターミナルを開く)
- [ステップ 3：プログラムをパソコンにダウンロードする](#ステップ-3プログラムをパソコンにダウンロードする)
- [ステップ 4：部品をそろえる（npm install）](#ステップ-4部品をそろえるnpm-install)
- [ステップ 5：Telegram のボットを作る](#ステップ-5telegram-のボットを作る)
- [ステップ 6：自分の宛先番号（chat_id）を調べる](#ステップ-6自分の宛先番号chat_idを調べる)
- [ステップ 7：設定ファイル（.env）を作る](#ステップ-7設定ファイルenvを作る)
- [ステップ 8：Telegram に届くかテストする](#ステップ-8telegram-に届くかテストする)
- [ステップ 9：データが取れているか確認する](#ステップ-9データが取れているか確認する)
- [ステップ 10：ボットを起動する](#ステップ-10ボットを起動する)
- [ステップ 11：24 時間動かし続ける](#ステップ-1124-時間動かし続ける)
- [使い方：Telegram から操作する](#使い方telegram-から操作する)
- [通知が多すぎる・少なすぎるときの調整](#通知が多すぎる少なすぎるときの調整)
- [困ったときは（エラー別）](#困ったときはエラー別)

---

## ステップ 0：全体像を知る

これから作るものは、こういう流れで動きます。

```
DexScreener（価格・出来高のデータ屋さん）
        ↓ 45秒〜4分ごとに自動でチェック
   あなたのパソコンの中のプログラム
        ↓ 条件に合ったコインを見つけたら
      Telegram にメッセージが届く
```

**用意するもの**

| 必要なもの | 説明 |
| --- | --- |
| パソコン（Mac か Windows） | プログラムを動かす場所 |
| Telegram のアカウント | 通知を受け取る場所。スマホアプリでも PC 版でも OK |
| インターネット接続 | 常時つながっている必要があります |

お金は一切かかりません（後述の「レンタルサーバー」を使う場合のみ月数百円〜）。

> 💡 **大事なこと**：プログラムが動いている間だけ通知が届きます。
> パソコンの電源を切ったりスリープすると止まります。対策はステップ 11 で説明します。

---

## ステップ 1：Node.js をインストールする

**Node.js（ノードジェイエス）** とは、このプログラムを動かすためのエンジンです。
これがないとプログラムは動きません。無料です。

### やること

1. ブラウザ（Chrome や Safari）で **https://nodejs.org/ja** を開きます
2. 大きなボタンが 2 つ並んでいます。**「LTS」と書いてある方** をクリックしてダウンロード
   - LTS = 安定版という意味です
   - バージョンが **22.13 以上** であることを確認してください（例: `22.20.0 LTS`）
3. ダウンロードされたファイルをダブルクリックして開きます
4. インストーラーが立ち上がるので、**「続ける」「同意する」「インストール」を押していくだけ** です
   - 途中で何か聞かれても、初期設定のまま進めて大丈夫です
   - Mac ではパソコンのパスワードを求められます。ログイン時のパスワードを入力してください
   - Windows で「Tools for Native Modules」というチェックボックスが出たら、**チェックは入れなくて OK**
5. 「インストールが完了しました」と出たら閉じます

これで準備完了です。ちゃんと入ったかは次のステップで確認します。

---

## ステップ 2：ターミナルを開く

**ターミナル** とは、文字でパソコンに命令する黒い画面のことです。
「難しそう」と思うかもしれませんが、**このガイドに書いてある文字をコピーして貼り付けて Enter を押すだけ** です。

### Mac の場合

1. キーボードの **`command` + `スペース`** を同時に押す（画面中央に検索窓が出ます）
2. `ターミナル` と入力
3. 出てきた「ターミナル」を Enter で開く

### Windows の場合

1. キーボードの **Windows キー** を押す
2. `powershell` と入力
3. 「Windows PowerShell」を Enter で開く

### 開いたら、動作確認をします

黒い（または白い）画面が出たら、次の 1 行をコピーして貼り付け、**Enter** を押してください。

```
node -v
```

> **貼り付け方**
> - Mac: `command` + `V`
> - Windows: `Ctrl` + `V`、または画面上で**右クリック**

`v22.20.0` のように **v22 以上の数字** が表示されれば成功です 🎉

`command not found` や `認識されていません` と出た場合は、
**ターミナルを一度閉じて開き直してから**もう一度試してください。それでもダメならステップ 1 をやり直します。

---

## ステップ 3：プログラムをパソコンにダウンロードする

GitHub（プログラムの保管場所）から、ボットのファイル一式をダウンロードします。

### いちばん簡単な方法（ZIP でダウンロード）

1. ブラウザで **https://github.com/cony-horizon/rhbot** を開きます
2. 緑色の **「Code」** ボタン → **「Download ZIP」** をクリック
3. ダウンロードされた ZIP ファイルをダブルクリックして解凍します
4. 解凍してできたフォルダを、**デスクトップに移動** します
   - フォルダ名が `rhbot-main` になっていたら、`rhbot` に変更しておくと後が楽です

### ターミナルでそのフォルダに移動する

ターミナルに戻って、次のように入力します。

**Mac の場合（おすすめの裏ワザ）**

1. ターミナルに `cd ` と入力（**cd のあとに半角スペース**を入れる。まだ Enter は押さない）
2. Finder で解凍したフォルダを、**ターミナルの画面にドラッグ&ドロップ**
3. フォルダの場所が自動で入力されるので **Enter**

**Windows の場合**

1. エクスプローラーで解凍したフォルダを開く
2. 上のアドレスバーをクリックして、表示されたパスをコピー
3. ターミナルで `cd ` と入力してから、コピーしたパスを貼り付けて Enter

### 移動できたか確認

```
ls
```

（Windows でも `ls` で動きます）

`package.json` `src` `README.md` などの名前が並べば成功です。

> ⚠️ **ここからのステップは、すべてこのフォルダの中で作業します。**
> ターミナルを閉じてしまったら、もう一度この `cd` からやり直してください。

---

## ステップ 4：部品をそろえる（npm install）

プログラムが動くのに必要な部品（ライブラリ）を自動でダウンロードします。

ターミナルに次を入力して Enter：

```
npm install
```

1〜3 分ほど待ちます。文字がたくさん流れますが、**気にしなくて大丈夫** です。

`added 〇〇 packages` のような表示が出て、入力待ちに戻れば成功です。

> 💡 黄色い `warn` の文字が出ても問題ありません。赤い `ERR!` が出た場合だけ、
> [困ったときは](#困ったときはエラー別) を見てください。

---

## ステップ 5：Telegram のボットを作る

「ボット」は Telegram の中の自動アカウントです。これがあなたに通知を送ってくれます。

### やること

1. Telegram（スマホでも PC でも OK）を開きます
2. 上の検索窓に **`@BotFather`** と入力して検索
3. **青いチェックマークが付いている「BotFather」** を選んでトークを開く
   - ⚠️ 偽物がたくさんいます。**必ず青い認証マークがあるもの**を選んでください
4. 画面下の **「開始」または `/start`** を送信
5. **`/newbot`** と送信します
6. BotFather が「ボットの名前は？」と聞いてきます → 好きな名前を送信
   - 例：`ミームコイン監視`（日本語でも OK。これは表示名です）
7. 次に「ユーザー名は？」と聞かれます → **必ず `bot` で終わる英数字** を送信
   - 例：`my_memecoin_spike_bot`
   - 「もう使われています」と言われたら、末尾に数字を足すなどして別の名前を試します
8. 成功すると、こんなメッセージが返ってきます：

```
Done! Congratulations on your new bot.
...
Use this token to access the HTTP API:
1234567890:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw
```

9. この **長い文字列（トークン）をコピー** して、メモ帳などに貼り付けて保管してください

> 🔐 **絶対に他人に見せないでください。**
> このトークンを知っている人は、あなたのボットを自由に操作できます。
> SNS やスクリーンショットにも写り込まないよう注意してください。

---

## ステップ 6：自分の宛先番号（chat_id）を調べる

ボットに「誰に送ればいいか」を教える必要があります。それが **chat_id** という番号です。

### やること

1. **まず、作ったボットに話しかけます**（これをしないと番号が取れません）
   - Telegram の検索窓で、さっき決めたユーザー名（例：`my_memecoin_spike_bot`）を検索
   - トークを開いて **「開始」** を押すか、`こんにちは` など何でもいいのでメッセージを送る
2. ブラウザ（Chrome など）のアドレスバーに、次の URL を入力して開きます

```
https://api.telegram.org/bot【ここにトークン】/getUpdates
```

**具体例**：トークンが `1234567890:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw` なら

```
https://api.telegram.org/bot1234567890:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw/getUpdates
```

> ⚠️ `bot` とトークンの間にスペースや記号は入れません。`bot` + トークンをそのままくっつけます。

3. 画面に文字がたくさん出ます。その中から **`"chat":{"id":` のすぐ後ろの数字** を探します

```
"chat":{"id":987654321,"first_name":"あなたの名前",...
              ↑ この数字（987654321）が chat_id
```

4. この数字をメモしてください

> 💡 **`{"ok":true,"result":[]}` としか出ない場合**
> ボットにメッセージを送れていません。ステップ 6 の 1. に戻って、ボットに話しかけてから再読み込みしてください。

> 💡 **グループに通知したい場合**
> ボットをグループに招待してからグループ内で発言し、同じ URL を開きます。
> グループの ID は `-1001234567890` のように **マイナスから始まる数字** になります。

---

## ステップ 7：設定ファイル（.env）を作る

トークンと chat_id をプログラムに教えるためのファイルを作ります。

### 7-1. ひな形をコピーする

ターミナルで（さっきのフォルダにいる状態で）次を実行：

**Mac の場合**
```
cp .env.example .env
```

**Windows の場合**
```
copy .env.example .env
```

### 7-2. ファイルを開いて編集する

**Mac の場合**
```
open -e .env
```
（テキストエディットが開きます）

**Windows の場合**
```
notepad .env
```
（メモ帳が開きます）

> ⚠️ **Word や Pages では絶対に開かないでください。** 書式が付いて壊れます。

### 7-3. 2 か所だけ書き換える

ファイルの中に、次の 2 行があります。`=` の**右側に何も書かれていない**状態です。

```
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
```

ここに、ステップ 5 と 6 で控えた値を **= の直後に、スペースを入れずに** 貼り付けます。

```
TELEGRAM_BOT_TOKEN=1234567890:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw
TELEGRAM_CHAT_ID=987654321
```

**よくある間違い**

| ❌ 間違い | ⭕ 正しい |
| --- | --- |
| `TELEGRAM_CHAT_ID = 987654321`（= の前後にスペース） | `TELEGRAM_CHAT_ID=987654321` |
| `TELEGRAM_BOT_TOKEN="1234..."`（引用符を付けた） | `TELEGRAM_BOT_TOKEN=1234...` |
| `TELEGRAM_BOT_TOKEN=bot1234...`（bot を付けた） | `TELEGRAM_BOT_TOKEN=1234...` |

他の行は**触らなくて大丈夫です**。あとで調整できます。

### 7-4. 保存する

- Mac: `command` + `S`
- Windows: `Ctrl` + `S`

> ⚠️ **Windows のメモ帳を使った人へ：最重要の注意点**
> メモ帳の「名前を付けて保存」を使うと、勝手に `.env.txt` という名前になってしまい、
> プログラムがファイルを見つけられません。
> 上記の `copy` コマンドで作ってから `Ctrl+S` で上書き保存すれば大丈夫です。
> 心配な場合は、ターミナルで `dir .env*` と実行して `.env` だけがあることを確認してください。

---

## ステップ 8：Telegram に届くかテストする

いよいよ最初のテストです。ターミナルで次を実行：

```
npm run probe -- telegram
```

**成功すると**、Telegram にこんなメッセージが届きます：

> ✅ テストメッセージ: スパイク検知ボットから送信できています

届きましたか？ 🎉 いちばんの難関はこれで突破です。

**届かなかった場合**は、ターミナルに出ているエラーを見てください：

| エラーの文字 | 原因と対処 |
| --- | --- |
| `Unauthorized` | トークンが間違っています。ステップ 5 のトークンをコピーし直してください |
| `chat not found` | chat_id が間違っているか、ボットに一度も話しかけていません。ステップ 6 をやり直し |
| `TELEGRAM_BOT_TOKEN が未設定です` | `.env` が読めていません。ファイル名が `.env.txt` になっていないか確認 |

---

## ステップ 9：データが取れているか確認する

次に、コインのデータがちゃんと取れるかを確認します。

```
npm run probe
```

こんな表示が出ます：

```
== DexScreener search (chainId=robinhood) ==
q="robinhood": 30 件中 12 件が robinhood
  robinhood  uniswap   $CASHCAT   age=3日2時間   1h=$45.0K  24h=$1.20M  liq=$180.0K  0x...
...
== RPC https://rpc.mainnet.chain.robinhood.com ==
chainId=4663 latestBlock=12345678
直近 2000 ブロックのプール作成イベント: 3 件
```

**ここで見るポイント**

- `〇件が robinhood` の数字が **1 以上** あれば、データ取得は成功しています
- `chainId=4663` と出れば、チェーンへの接続も成功です

> 💡 **`RPC ERROR` と出た場合**
> チェーンへの直接接続だけが失敗しています。**このままでも動きます**（DexScreener のデータだけで検知します）。
> 気になる場合は `.env` の `RPC_URL=` の行を空っぽ（`RPC_URL=` だけ）にしておくとエラーが出なくなります。

> 💡 **`0 件が robinhood` ばかりの場合**
> DexScreener 上のチェーン名が変わった可能性があります。`.env` の `CHAIN_ID=robinhood` を確認してください。

---

## ステップ 10：ボットを起動する

準備完了です。次を実行：

```
npm run build
```

（10 秒ほど。これはプログラムを実行できる形に変換する作業です）

続いて：

```
npm start
```

**起動すると**、Telegram に

> 🟢 スパイク検知ボット起動 (chain=robinhood, 監視 0 ペア)

と届き、ターミナルには次のような行が流れ続けます：

```
2026-09-04T18:30:00.000Z [INFO ] discovery: 45 ペア取得, 45 件を新規追加 (合計 45)
```

**これで完成です。** あとは条件に合ったコインが現れたら、自動で通知が届きます 🎉

最初の数分は監視対象を集めている時間なので、通知が来なくても正常です。

### 止めたいとき

ターミナルの画面で **`Ctrl` + `C`**（Mac も Ctrl です。command ではありません）を押します。

### もう一度動かしたいとき

ターミナルを開いて、フォルダに移動してから `npm start` だけで OK です。
（`npm install` や `npm run build` は、プログラムを更新したとき以外は不要です）

---

## ステップ 11：24 時間動かし続ける

このボットは **動いている間しか通知しません**。半日〜2 日後の復活を捉えるのが目的なので、
つけっぱなしにする方法を決めましょう。

### 方法 A：自分のパソコンをつけっぱなしにする（無料・すぐできる）

いちばん簡単ですが、**スリープすると止まります**。スリープを無効にしてください。

**Mac**
1. 「システム設定」→「ロック画面」
2. 「使用していないときにディスプレイをオフにする」→ **「しない」**
3. さらに「バッテリー」→「オプション」→「ディスプレイがオフのときに自動スリープさせない」を **オン**

**Windows**
1. 「設定」→「システム」→「電源とバッテリー」
2. 「画面とスリープ」→ **「デバイスをスリープ状態にする」を「なし」** に

ノートパソコンは**電源に繋いだまま**にしてください。

### 方法 B：レンタルサーバー（VPS）で動かす（月 500 円〜・確実）

パソコンを閉じても動き続けます。長く使うならこちらがおすすめです。

1. VPS を契約します（初心者向け：**さくらの VPS**、**ConoHa VPS**、海外なら **Hetzner**、**Vultr**）
   - いちばん安いプラン（メモリ 1GB）で十分です
2. 契約時に **Ubuntu 24.04** を選びます
3. 契約後に案内される「SSH 接続」の手順でサーバーに接続します
4. 接続できたら、次を順に貼り付けて実行します：

```
sudo apt update
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt install -y nodejs git
```

（`node -v` で `v22...` と出れば成功です）

```
git clone https://github.com/cony-horizon/rhbot.git rhbot
cd rhbot
npm install
```

```
nano .env
```
（`.env.example` の中身を貼り付けて、トークンと chat_id を書き込み、`Ctrl+X` → `Y` → `Enter` で保存）

```
npm run build
sudo npm install -g pm2
pm2 start npm --name rhbot -- start
pm2 save
pm2 startup
```

最後の `pm2 startup` が出力したコマンドをもう一度貼り付けて実行すると、
**サーバーを再起動しても自動で立ち上がる**ようになります。

動作確認は `pm2 logs rhbot`、止めるときは `pm2 stop rhbot` です。

---

## 使い方：Telegram から操作する

ボットとのトークで、次のコマンドを送れます。

| 送るコマンド | 何が起きるか |
| --- | --- |
| `/status` | 今いくつのコインを監視しているか、直近 24 時間の通知数 |
| `/top` | 今いちばん出来高が多いコインの一覧 |
| `/top 20` | 上位 20 件を表示 |
| `/alerts` | 直近の通知履歴（見逃したときに便利） |
| `/test` | 通知の見本を送る。ちゃんと届くか確かめたいときに |
| `/watch 0xアドレス` | 特定のコインを必ず監視対象に入れる |
| `/unwatch 0xアドレス` | 監視から外す |
| `/list` | 手動で追加したコインの一覧 |
| `/filtered` | スキャムと判定して止めた通知を見る。判定が厳しすぎないか確認できます |
| `/why 0xアドレス` | 気になる銘柄のリスクを採点して、内訳を見せます |
| `/mute 60` | 60 分だけ通知を止める（寝るとき用。検知自体は続きます） |
| `/unmute` | 通知を再開 |
| `/config` | 今の検知設定を確認 |
| `/help` | コマンド一覧 |

### 届く通知の見本

```
🔥 復活スパイク検知
$CASHCAT — Cash Cat  /WETH
DEX: uniswap (v3) | 経過: 2日6時間
価格: $0.0004521  (5m +8.2% / 1h +42.0% / 安値比 +67.3%)
出来高: 1h $85.0K | 24h $110.0K | 突発率 32.6x
流動性: $220.0K | FDV: $4.52M
取引 1h: 買 142 / 売 38
理由: 価格 +67% (120分安値比) / 1h 出来高 $85,000 は直前平均の 32.6x
📈 DexScreener で開く
CA: 0x1234...
```

**「突発率 32.6x」** が、あなたが探している「急に出来高が湧いた」度合いです。
数字が大きいほど「今まで静かだったのに急に動き出した」という意味になります。

---

## 通知が多すぎる・少なすぎるときの調整

`.env` を開いて数字を変え、保存して**ボットを再起動**（`Ctrl+C` → `npm start`）すれば反映されます。

### 通知が多すぎる（ノイズが多い）とき

`.env` の中の該当行を、次のように書き換えます。

```
REVIVAL_PRICE_CHANGE_PCT=40      ← 30 から上げる（40%以上の上昇だけ通知）
REVIVAL_VOL_SPIKE_RATIO=5        ← 3 から上げる（本当に突発的なものだけ）
REVIVAL_MIN_VOL_H1_USD=30000     ← 10000 から上げる（小さすぎる銘柄を除外）
MIN_LIQUIDITY_USD=20000          ← 5000 から上げる（流動性が薄いものを除外）
```

### 通知が少なすぎる（見逃している気がする）とき

```
REVIVAL_PRICE_CHANGE_PCT=25      ← 30 から下げる
REVIVAL_VOL_SPIKE_RATIO=2        ← 3 から下げる
REVIVAL_MIN_VOL_H1_USD=5000      ← 10000 から下げる
DORMANT_POLL_INTERVAL_SEC=120    ← 240 から下げる（チェック頻度を上げて反応を早く）
```

### 同じコインの通知がしつこいとき

```
REVIVAL_COOLDOWN_MIN=360         ← 180 から上げる（同じ銘柄は6時間空ける）
REVIVAL_ESCALATION_PCT=0         ← 追加上昇の通知をオフにする
```

### 新規ローンチの通知だけ止めたいとき

```
NEW_LAUNCH_ENABLED=false
```

### スキャムが通知に混ざるとき

バンドルや洗浄取引で出来高を作った銘柄は自動で除外していますが、すり抜けたら厳しくできます。

```
SCAM_SCORE_THRESHOLD=35          50 から下げる（厳しくなる）
SCAM_CHURN_MID=3                 4 から下げる（出来高の多さに敏感になる）
```

逆に**ほしかった銘柄まで消えている**場合は緩めます。まず `/filtered` で何が止まったかを見てください。

```
SCAM_SCORE_THRESHOLD=70          50 から上げる（通りやすくなる）
```

> 💡 **おすすめの進め方**
> まずは初期設定のまま 1〜2 日動かして、`/alerts` で「どんな通知が来たか」を眺めてください。
> そのうえで「これは要らなかった」と思う通知の傾向を見て、上の数字を調整するのが確実です。

---

## 困ったときは（エラー別）

### `tsx: command not found` / `vitest: command not found`

部品（ライブラリ）が入っていない状態です。**もう一度同じコマンドを実行してください。**
自動で入れ直してから処理を続けます。

それでも直らないときは、次を実行してください。

```
npm install --include=dev
```

### `EACCES: permission denied` / `npm error code EEXIST`

npm の保管フォルダが `root` の持ち物になっていて、書き込めない状態です。
過去に `sudo npm install` を実行すると、こうなります。

**Mac の場合**、次を 1 行ずつ実行してください。

```
sudo chown -R $(whoami) ~/.npm
rm -rf node_modules
npm install --include=dev
```

1 行目で Mac のログインパスワードを聞かれます。
入力しても画面には何も表示されませんが、そのまま `Enter` を押して大丈夫です。

**Windows の場合**は、PowerShell を「管理者として実行」で開き直してから、
`npm cache clean --force` → `npm install --include=dev` を実行します。

> ⚠️ **以後 `npm` に `sudo` を付けないでください。** これがこの状態の原因です。
> `npm install` は `sudo` なしで実行するのが正しい使い方です。

### `npm: command not found` / `npm は認識されていません`

Node.js が入っていないか、ターミナルが古い状態です。
**ターミナルを閉じて開き直す** → それでもダメならステップ 1 をやり直してください。

### `no such file or directory` / `指定されたパスが見つかりません`

フォルダの場所が違います。ステップ 3 の `cd` からやり直してください。
今どこにいるかは `pwd` と入力すれば確認できます（Mac / Windows 共通）。

### `TELEGRAM_BOT_TOKEN が未設定です`

`.env` ファイルが正しく読めていません。次を実行して `.env` があるか確認します。

Mac: `ls -a | grep env` / Windows: `dir .env*`

`.env.txt` になっていたら、それが原因です。名前を `.env` に直してください。

### `Unauthorized`（Telegram）

トークンが間違っています。BotFather のトークで `/mybots` と送り、自分のボットを選ぶと
「API Token」から再確認できます。

### `chat not found`（Telegram）

- ボットに一度も話しかけていない → ボットのトークで「開始」を押す
- chat_id の数字が違う → ステップ 6 をやり直す

### `429` や `rate limited` がログに出る

データ屋さんへのアクセスが多すぎます。プログラムが自動で待つので**放置して大丈夫**です。
頻発する場合は `.env` の `POLL_INTERVAL_SEC` を `60` に、`DORMANT_POLL_INTERVAL_SEC` を `300` に上げてください。

### `RPC ERROR` がログに出続ける

チェーンへの直接接続だけが失敗しています。動作自体には影響しません。
消したい場合は `.env` の `RPC_URL=` の行を、`RPC_URL=` だけの空の状態にしてください。

### 通知が一切来ない（エラーも出ていない）

1. **`/test` を送る** → 見本の通知が届けば、配信経路は正常です
2. **`/status` を送る** → 監視ペア数が 0 でないか確認します
3. 0 件のままならステップ 9 の `npm run probe` を実行します。
   `CHAIN_ID` が違っていた場合は、正しい候補が一覧で表示されます
4. ペアがあるのに通知が来ないなら、単に条件を満たすコインがまだ現れていないだけです。
   試しに `.env` で `REVIVAL_PRICE_CHANGE_PCT=10`、`REVIVAL_VOL_SPIKE_RATIO=1.5` にして再起動すると
   通知が来ることを確認できます（確認できたら必ず元に戻してください）

> 💡 監視対象を 5 回続けて 1 件も取得できなかった場合は、ボット自身が Telegram に警告を送ります。
> 黙って壊れたままになることはありません。

### それでも解決しないとき

ターミナルに出ている**赤い文字の行をそのままコピー**して質問すれば、原因を特定できます。
その際、**トークンの文字列は消してから**共有してください。

---

## 最後に

- このボットは **通知するだけ** で、自動売買はしません。売買の判断はご自身で行ってください
- 通知が来たコインが必ず上がるわけではありません。ラグ（開発者の持ち逃げ）にも十分ご注意ください
- 設定の意味をもっと詳しく知りたい場合は [README.md](./README.md) を見てください
